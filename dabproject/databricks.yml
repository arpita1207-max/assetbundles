# This is a Databricks asset bundle definition for dabproject.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dabproject
  uuid: 68ea9531-7d26-4a12-b6ee-abd8448b6fef


include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    presets:
      name_prefix: ${workspace.current_user.short_name}
      source_linked_deployment: false
    workspace:
      host: https://dbc-86810a94-2eb5.cloud.databricks.com/

  prod:
    mode: production
    workspace:
      host: https://dbc-86810a94-2eb5.cloud.databricks.com/
      # We explicitly deploy to /Workspace/Users/arpita1207sinha@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/arpita1207sinha@gmail.com/PROD/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - group_name: admins
        level: CAN_MANAGE
